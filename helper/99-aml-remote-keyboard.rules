# Remote Keyboard helper — udev for /dev/uinput (Ubuntu).
# Install: sudo cp 99-aml-remote-keyboard.rules /etc/udev/rules.d/ && sudo udevadm control --reload-rules
KERNEL=="uinput", MODE="0660", GROUP="input"
